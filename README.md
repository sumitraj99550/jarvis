# JARVIS — AI Operating System

Phase 1: Foundation scaffold. See project chat history for the full 20-phase roadmap.

## Getting Started

```bash
npm install
docker compose up -d   # starts Postgres + Redis
cp .env.example .env.local
npm run dev
```

Open http://localhost:3000.
